import styles from "./audit.module.css";
type AuditEvent={id:string;occurredAtUtc:string;actorId:string;action:string;entityType:string;entityId:string;correlationId:string};export const dynamic="force-dynamic";
export default async function AuditPage(){let items:AuditEvent[]=[];let error:string|null=null;try{const r=await fetch("http://localhost:5120/api/v1/audit-events?take=100",{cache:"no-store"});if(!r.ok)throw new Error();items=((await r.json()) as {items:AuditEvent[]}).items}catch{error="โหลด Audit History ไม่สำเร็จ"}return <main><header className="pageHead"><div><h1>Audit History</h1><p>ประวัติการเปลี่ยนแปลงแบบ append-only สำหรับการตรวจสอบย้อนหลัง</p></div><span className="statusPill">Audit & compliance</span></header>{error&&<div className={styles.error}>{error}</div>}<section className={styles.panel}><div className={styles.head}><h2>เหตุการณ์ล่าสุด</h2><strong>{items.length} events</strong></div>{items.length===0?<p className={styles.empty}>ยังไม่มี Audit Event</p>:<div className={styles.tableWrap}><table><thead><tr><th>วันเวลา</th><th>เหตุการณ์</th><th>ผู้ดำเนินการ</th><th>Correlation ID</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{new Date(x.occurredAtUtc).toLocaleString("th-TH")}</td><td><strong>{x.entityType} · {x.action}</strong><small>{x.entityId}</small></td><td>{x.actorId}</td><td><code>{x.correlationId}</code></td></tr>)}</tbody></table></div>}</section></main>}
