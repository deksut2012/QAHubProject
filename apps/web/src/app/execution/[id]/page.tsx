import{CycleRunner}from"./runner";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;let cycle=null,error:string|null=null;try{const r=await fetch(`http://localhost:5120/api/v1/execution/cycles/${id}`,{cache:"no-store"});if(!r.ok)throw new Error();cycle=await r.json()}catch{error="โหลด Test Cycle ไม่สำเร็จ"}return <main><header className="pageHead"><div><h1>{cycle?.name??"Test Cycle"}</h1><p>บันทึกผลทดสอบ แนบหลักฐาน และ re-run ได้ในจุดเดียว</p></div><span className="statusPill">{cycle?.status??"Unavailable"}</span></header>{cycle?<CycleRunner initialCycle={cycle}/>:<p>{error}</p>}</main>}
