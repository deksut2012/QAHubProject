"use client";
import {FormEvent,useMemo,useState} from "react";import styles from "./requirements.module.css";
type Product={id:string;code:string;name:string};type Requirement={id:string;productId:string;moduleId?:string;jobNumber:string;title:string;description?:string;acceptanceCriteria?:string;assignee?:string;status:string;updatedAtUtc:string};
export function RequirementsWorkspace({initialProducts,initialRequirements,initialError}:{initialProducts:Product[];initialRequirements:Requirement[];initialError:string|null}){const[items,setItems]=useState(initialRequirements),[error,setError]=useState(initialError),[query,setQuery]=useState(""),[statusFilter,setStatusFilter]=useState(""),[open,setOpen]=useState(false),[saving,setSaving]=useState(false);const[productId,setProductId]=useState(initialProducts[0]?.id??""),[jobNumber,setJobNumber]=useState(""),[title,setTitle]=useState(""),[description,setDescription]=useState(""),[criteria,setCriteria]=useState(""),[assignee,setAssignee]=useState("");const shown=useMemo(()=>items.filter(x=>(!query||`${x.jobNumber} ${x.title}`.toLowerCase().includes(query.toLowerCase()))&&(!statusFilter||x.status===statusFilter)),[items,query,statusFilter]);async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError(null);try{const r=await fetch("/backend/v1/requirements",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({productId,jobNumber,title,description,acceptanceCriteria:criteria,assignee})});if(r.status===409)throw new Error("Job Number นี้มีอยู่แล้วใน Product");if(!r.ok)throw new Error("บันทึก Requirement ไม่สำเร็จ");const created=await r.json() as Requirement;setItems(v=>[created,...v]);setJobNumber("");setTitle("");setDescription("");setCriteria("");setAssignee("");setOpen(false)}catch(x){setError(x instanceof Error?x.message:"เกิดข้อผิดพลาด")}finally{setSaving(false)}}return <><div className={styles.toolbar}><div className={styles.filters}><input aria-label="ค้นหา Requirement" placeholder="ค้นหา Job Number หรือชื่อ..." value={query} onChange={e=>setQuery(e.target.value)}/><select aria-label="กรองสถานะ" value={statusFilter} onChange={e=>setStatusFilter(e.target.value)}><option value="">ทุกสถานะ</option>{["Draft","InReview","NeedsRevision","Approved","Implemented","Verified","Closed","Superseded"].map(x=><option key={x}>{x}</option>)}</select></div><button onClick={()=>setOpen(v=>!v)}>+ New Requirement</button></div>{error&&<p className={styles.error}>{error}</p>}{open&&<form className={styles.form} onSubmit={submit}><div className={styles.formHead}><div><h2>New Requirement</h2><p>สร้าง Requirement ในสถานะ Draft</p></div><button type="button" onClick={()=>setOpen(false)}>×</button></div><div className={styles.formGrid}><label>Product<select value={productId} onChange={e=>setProductId(e.target.value)} required>{initialProducts.map(x=><option value={x.id} key={x.id}>{x.code} · {x.name}</option>)}</select></label><label>Job Number<input value={jobNumber} onChange={e=>setJobNumber(e.target.value)} maxLength={50} placeholder="JOB-2026-001" required/></label><label className={styles.full}>Title<input value={title} onChange={e=>setTitle(e.target.value)} maxLength={250} required/></label><label className={styles.full}>Description<textarea value={description} onChange={e=>setDescription(e.target.value)} required/></label><label className={styles.full}>Acceptance Criteria<textarea value={criteria} onChange={e=>setCriteria(e.target.value)} placeholder="Given / When / Then หรือรายการเงื่อนไข" required/></label><label>Assignee<input value={assignee} onChange={e=>setAssignee(e.target.value)} placeholder="ชื่อหรืออีเมล"/></label></div><div className={styles.formActions}><button type="submit" disabled={saving}>{saving?"กำลังบันทึก...":"Create Requirement"}</button></div></form>}<section className={styles.card}><div className={styles.cardHead}><h2>Requirement directory</h2><span>{shown.length} items</span></div>{shown.length===0?<p className={styles.empty}>ยังไม่มี Requirement ที่ตรงกับตัวกรอง</p>:<div className={styles.tableWrap}><table><thead><tr><th>Job Number</th><th>Requirement</th><th>Product</th><th>Assignee</th><th>Status</th><th>Updated</th></tr></thead><tbody>{shown.map(x=><tr key={x.id}><td><strong>{x.jobNumber}</strong></td><td>{x.title}</td><td>{initialProducts.find(p=>p.id===x.productId)?.code??"-"}</td><td>{x.assignee??"Unassigned"}</td><td><span className={styles.status}>{x.status}</span></td><td>{new Date(x.updatedAtUtc).toLocaleDateString("th-TH")}</td></tr>)}</tbody></table></div>}</section></>}
