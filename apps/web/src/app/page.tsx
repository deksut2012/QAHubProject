import Link from"next/link";import styles from"./page.module.css";type Named={name:string;count:number};type Trend={date:string;passed:number;failed:number;blocked:number};type Work={assignee:string;testItems:number;openBugs:number};type Dashboard={activeProducts:number;requirements:number;testCases:number;openBugs:number;criticalHighBugs:number;requirementCoverage:number;execution:{total:number;executed:number;passed:number;failed:number;blocked:number;skipped:number;passRate:number};requirementStatuses:Named[];bugSeverities:Named[];executionTrend:Trend[];workload:Work[];releaseCount:number;averageReleaseReadiness:number};export const dynamic="force-dynamic";export default async function Home(){let data:Dashboard|null=null,error:string|null=null;try{const r=await fetch("http://localhost:5120/api/v1/dashboard",{cache:"no-store"});if(!r.ok)throw new Error();data=await r.json()}catch{error="โหลดข้อมูล Dashboard ไม่สำเร็จ"}if(!data)return <main><h1>QA Dashboard</h1><p className={styles.error}>{error}</p></main>;const metrics=[{label:"Active Products",value:data.activeProducts,tone:"blue"},{label:"Execution Pass Rate",value:`${data.execution.passRate}%`,tone:"green"},{label:"Critical / High Bugs",value:data.criticalHighBugs,tone:"red"},{label:"Release Readiness",value:`${data.averageReleaseReadiness}%`,tone:"orange"}];const trendMax=Math.max(1,...data.executionTrend.map(x=>x.passed+x.failed+x.blocked));return <main><header className="pageHead"><div><h1>QA Dashboard</h1><p>ภาพรวมคุณภาพจาก transaction จริงทุกผลิตภัณฑ์</p></div><div className="pageHeadActions"><a className="statusPill" href="/backend/v1/dashboard/report">Export CSV</a><span className="statusPill">Phase 7 · Reporting</span></div></header><section className={styles.metrics}>{metrics.map(x=><article className={`prototypeCard ${styles.metric}`} key={x.label}><div><p>{x.label}</p><h2>{x.value}</h2></div><span className={styles[x.tone]}>●</span></article>)}</section><section className={styles.columns}><article className="prototypeCard"><div className={styles.sectionTitle}><div><h2>Execution trend · 14 days</h2><p>Passed / Failed / Blocked attempts</p></div></div><div className={styles.chart}>{data.executionTrend.map(x=><div key={x.date} title={`${x.date}: P${x.passed} F${x.failed} B${x.blocked}`}><i className={styles.pass} style={{height:`${x.passed/trendMax*100}%`}}/><i className={styles.fail} style={{height:`${x.failed/trendMax*100}%`}}/><i className={styles.blocked} style={{height:`${x.blocked/trendMax*100}%`}}/><small>{new Date(x.date).getDate()}</small></div>)}</div></article><article className="prototypeCard"><div className={styles.sectionTitle}><div><h2>Coverage & progress</h2><p>Requirement and execution summary</p></div></div><div className={styles.readiness}><div><span>Requirement coverage</span><strong>{data.requirementCoverage}%</strong></div><div><span>Test Cases</span><strong>{data.testCases}</strong></div><div><span>Executed</span><strong>{data.execution.executed}/{data.execution.total}</strong></div><div><span>Open bugs</span><strong>{data.openBugs}</strong></div></div></article></section><section className={styles.columns}><article className="prototypeCard"><div className={styles.sectionTitle}><div><h2>Bug severity</h2><p>Open defects requiring attention</p></div><Link href="/bugs">Open Kanban →</Link></div><div className={styles.breakdown}>{data.bugSeverities.map(x=><div key={x.name}><b>{x.count}</b><span>{x.name}</span></div>)}</div></article><article className="prototypeCard"><div className={styles.sectionTitle}><div><h2>QA workload</h2><p>Assigned test items and open bugs</p></div></div><div className={styles.workload}>{data.workload.map(x=><div key={x.assignee}><span>{x.assignee}</span><b>{x.testItems} tests · {x.openBugs} bugs</b></div>)}{data.workload.length===0&&<p>No assigned work</p>}</div></article></section></main>}
